Drawio.conf

upstream drawio {
    server drawio:8080;
    keepalive 32;
}

# map doit être dans le contexte http ; comme ce fichier est inclus dans http{}, c'est OK
map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

server {
    listen 80;
    server_name drawio.ALPHA.DOM.local;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name drawio.ALPHA.DOM.local;

    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;

    location / {
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_pass http://drawio;
    }
}

-------------------------------------------------------

upstream drawio { server drawio:8080; keepalive 32; }

map $http_upgrade $connection_upgrade { default upgrade; '' close; }

server {
    listen 80;
    server_name drawio.ALPHA.DOM.local;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name drawio.ALPHA.DOM.local;

    ssl_certificate /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;
    ssl_trusted_certificate /etc/nginx/certs/fullchain.pem;

    location / {
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Port $server_port;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
        proxy_redirect off;
        proxy_buffering off;
        proxy_pass http://drawio;
        limit_except GET HEAD POST { deny all; }
    }
}
